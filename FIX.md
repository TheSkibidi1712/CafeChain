[URGENT BUG FIX: BROKEN ACCESS CONTROL & UI ACTIVE STATE]Hệ thống bạn vừa code đang mắc phải một lỗi bảo mật phân quyền (Privilege Escalation) cực kỳ nghiêm trọng và một lỗi UI. Yêu cầu bạn dừng mọi việc và sửa ngay 2 vấn đề sau:🔥 BUG 1: LỖI BẢO MẬT "QUẢN LÝ CHI NHÁNH ĐƯỢC PHÉP SỬA/GIÁNG CHỨC ADMIN"Hiện tại, tài khoản Store Manager có thể mở form Edit của Admin System, và khi save lại, Admin System bị biến thành Cashier.Yêu cầu sửa Tầng Service (AdminStaffService - Hàm Update và ToggleStatus):BẮT BUỘC thêm logic kiểm tra chéo (Cross-check) trước khi Update/Toggle: Tìm thông tin của nhân viên đang bị chỉnh sửa (Target Staff) trong DB cùng với các quyền (AccountRoles) của họ.Luật Thép: Nếu User đang thao tác là Store Manager, và Target Staff đang giữ các chức vụ cấp cao (RoleId = 5 Admin, RoleId = 4 Province, RoleId = 3 Ward, hoặc RoleId = 2 Store Manager) $\rightarrow$ NÉM NGAY EXCEPTION: "Bạn không có quyền chỉnh sửa tài khoản cấp cao hơn hoặc ngang hàng!"Lưu ý: Store Manager CHỈ ĐƯỢC PHÉP edit những tài khoản có Role cao nhất là Cashier (RoleId = 1) và thuộc đúng StoreId của mình.🔥 BUG 2: ẨN NÚT THAO TÁC Ở TRANG INDEX (FRONTEND SECURITY)Không chỉ chặn ở Backend, mà ở Frontend (Index.cshtml), phải ẩn luôn nút "Sửa" và nút "Khóa" nếu User không có quyền tác động lên dòng đó.Cách làm: Truyền thêm một thuộc tính bool CanEdit vào StaffIndexVM. Trong Controller/Service, nếu Current User là Store Manager mà dòng đó là Admin thì CanEdit = false. Ở View HTML, dùng @if(item.CanEdit) để hiển thị 2 nút thao tác.🔥 BUG 3: SAI ACTIVE STATE Ở SIDEBARỞ _AdminLayout.cshtml, menu đang bị sáng đỏ ở "Quản lý danh mục" dù đang đứng ở trang Nhân sự.Yêu cầu: Sửa lại logic active CSS (class="active"). Phải check ViewContext.RouteData.Values["controller"].ToString() == "AdminStaff" thì thẻ <li> của Quản lý nhân sự mới được thêm class active.Hãy phản hồi lại bằng đoạn code C# của hàm Update trong AdminStaffService (có chứa đoạn check chặn quyền) và đoạn code sửa _AdminLayout.cshtml.